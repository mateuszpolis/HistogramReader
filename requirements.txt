matplotlib>=3.5.0

# Core dependencies (production)
numpy>=1.21.0
pandas>=1.3.0

# Optional dependencies
Pillow>=9.5.0  # Required for GIF export in grid visualization
plotly>=5.0.0   # Optional for interactive plots
pyarrow>=10.0.0  # Required for parquet file support
requests>=2.28.0  # Required for DA_batch_client
scipy>=1.7.0
seaborn>=0.11.0  # Optional for advanced plotting
tkinter-tooltip>=2.0.0

# Note: Development, testing, and documentation dependencies
# are defined in pyproject.toml under [project.optional-dependencies]
